# Requirements for comparison workspace
# These are additional dependencies beyond the main repository requirements.
# Install with: pip install -r comparison/requirements.txt

# Schema validation
jsonschema>=4.0.0

# Data analysis (optional but recommended)
pandas>=2.0.0

# Visualization (optional but recommended for notebooks)
matplotlib>=3.8.0

# Jupyter notebook support
notebook>=7.0.0
ipykernel>=6.0.0
